# 隐私说明

## 本地数据

以下数据保存在浏览器的 `chrome.storage.local`：

- 用户录入的个人资料和经历。
- 用户主动上传到管理台的头像照片文件内容。
- 简历版本、附件文件名和自定义字段。
- AI 服务地址、模型名称和 API Key。
- AI 自动回答规则。
- 网申记录、备注和字段匹配结果。
- 邮箱账号、IMAP 客户端授权码和飞书应用配置。
- 最近 7 天邮件的主题、分类、接收时间、DDL 和处理状态。

项目不包含遥测、广告或第三方统计代码。

## AI 字段映射

启用 AI 字段映射后，扩展会向用户配置的 AI 服务发送：

- 网页字段的 label、placeholder、id、name 和 autocomplete。
- 控件类型和可选项文本。
- 本地标准字段的 key、名称和别名。

字段映射请求不包含用户资料值。AI 只返回网页字段与标准字段之间的映射。

## 简历解析

用户主动选择文件并执行解析时，扩展会在本地读取 PDF、DOCX、TXT 或 Markdown，并将提取出的简历文本发送给用户配置的 AI 服务。请在使用前确认服务提供方的隐私政策。

## 头像照片

用户主动上传头像照片时，扩展会将图片内容保存在本机浏览器存储中，用于后续在招聘网站头像、照片或证件照上传控件中自动上传。扩展不会根据本地文件路径自动读取头像文件。

## 邮件待办

邮件待办通过用户主动安装的 Native Messaging 本地桥接读取 126 / 163 邮箱，
只查询 `INBOX` 最近 7 天。投递成功、简历创建、验证码和招聘宣传等邮件会在
本地过滤；只有疑似在线测评、AI 面试或面试邀约的邮件正文会发送给用户配置的
AI 服务，用于提取公司、分类、链接和 DDL。

结构化结果仅写入用户配置的飞书多维表格。扩展不上传邮箱授权码、飞书 Secret
或 AI Key 到项目维护者的服务。

## 网页访问

扩展只在用户打开弹窗并执行扫描或填写时读取当前页面表单。填写完成后不会自动保存、提交或投递。

## 删除数据

用户可以在扩展管理台逐项删除资料，也可以在浏览器扩展管理页移除扩展并清除本地数据。
Native Host 的去重数据库位于扩展目录 `native-host/data/`，运行
`./native-host/uninstall.sh` 可移除浏览器注册信息。
